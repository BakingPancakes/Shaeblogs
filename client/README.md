## TODO:

- figure out how to request and store images (for article thumbnails)
- empty article is defined in constant.ts and called in multiple parts of the database call pipeline, not sure if this is bad practice or if there's another way I should do this

## Dev notes:

If tsconfig changes are needed, add them to tsconfig.app.json instead of tsconfig.json (it's just the root config)
